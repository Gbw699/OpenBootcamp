// Notación
// ; = delimita el final de una línea
const b = 4;

// [] = se utilizan para las listas, arreglos o arrays
const ar = [1,2,3,4]
console.log(ar[2])

// () = se utilizan para funciones
function suma(a,b) {
    // acá se escribe la función
}

// {} = se utilizan para objetos, funicones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10
}

// . = se utiliza en los objetos para acceder a los atributos
movil.anchura
console.log(movil.altura)